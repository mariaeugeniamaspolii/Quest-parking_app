import React from 'react';
import { Text } from 'react-native';

export const MeteredTab = () => {
    return (
        <Text>
            MeteredTab
        </Text>
            )
};

export default MeteredTab;
