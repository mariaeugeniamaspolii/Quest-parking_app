const dimensions = ['small', 'medium', 'big'];
const subscription = ['platinum', 'gold', 'premium'];
const action = ['rent', 'buy'];
const rentMode = ['full', 'morning', 'evening'];
const servicesProperty = ['security', 'gatekeeper'];
const meteredCost = 24;

const state = ['approved', 'pending', 'rejected'];


module.exports = {
    dimensions,
    subscription,
    action,
    rentMode,
    servicesProperty,
    meteredCost,
    state,
};